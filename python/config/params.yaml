# Stock Screening System Configuration
# Based on Minervini's Stage Theory and VCP Pattern

# Data source settings
data:
  source: "yfinance"
  target_categories:
    - "sp500"           # S&P 500
    - "nasdaq"          # NASDAQ Composite
    - "nyse"            # NYSE Listed
  min_market_cap: 2_000_000_000  # $2B minimum
  min_price: 5.0
  min_volume: 500_000
  exclude_types: ["ETF", "REIT", "ADR"]
  history_period: "2y"  # 2 years of data
  max_tickers: 3500     # Maximum tickers to process

# Benchmark
benchmark:
  symbol: "SPY"  # S&P 500 ETF
  enabled: true  # Set to false to disable benchmark (RS condition auto-passed)

# Stage detection parameters
stage:
  sma_periods: [50, 150, 200]

  # STRICT MODE (default)
  strict:
    min_price_above_52w_low: 1.30      # 30% above 52-week low
    max_distance_from_52w_high: 0.75   # Within 25% of 52-week high
    rs_new_high_threshold: 0.95        # RS ≥ 95% of 52w high
    min_volume: 500000

  # RELAXED MODE (fallback)
  relaxed:
    min_price_above_52w_low: 1.20      # 20% above (relaxed)
    max_distance_from_52w_high: 0.60   # Within 40% of high (relaxed)
    rs_new_high_threshold: 0.90        # RS ≥ 90% (relaxed)
    min_volume: 300000                 # Lower volume

  # Fallback behavior
  auto_fallback_enabled: true
  min_trades_threshold: 1

  # Legacy parameters (for backward compatibility)
  min_price_above_52w_low: 1.30
  max_distance_from_52w_high: 0.75
  min_volume: 500_000

  min_slope_200_days: 20              # 200 MA must be rising for 20 days
  rs_min_rating: 70
  rs_new_high_required: true

# VCP detection parameters
vcp:
  base_period_min: 35
  base_period_max: 65
  contraction_sequence: [0.25, 0.15, 0.08, 0.05]
  last_contraction_max: 0.10
  dryup_vol_ratio: 0.6
  breakout_vol_ratio: 1.5
  pivot_min_high_52w_ratio: 0.95
  pivot_buffer_atr: 0.5

# Entry conditions (daily evaluation during backtest)
# NOTE: Entry conditions are SEPARATE from Stage2 conditions.
# Stage2 is for universe selection (one-time at start).
# Entry conditions are lightweight checks for daily trade decisions.
# Entry conditions do NOT include rs_new_high (that's a Stage2 condition).
entry:
  buy_zone_pct: 0.03                 # Within 3% of pivot
  breakout_vol_ratio: 1.5            # Volume 1.5x 50-day average
  min_volume: 500000                 # Minimum 50-day avg volume for entry

# Risk management
risk:
  risk_per_trade: 0.0075             # 0.75% of capital per trade
  max_loss_hard: 0.10                # 10% hard stop
  initial_stop_max: 0.07             # 7% maximum initial stop

# Exit conditions
exit:
  partial_sell_bb_ratio: 0.25        # Sell 25% at BB upper
  sma_50_exit: true                  # Exit on 50 MA break
  trail_stop_atr: 2.0

# Fundamentals filter settings (Minervini growth criteria)
fundamentals:
  enabled: true
  min_eps_growth: 0.25              # 25% minimum EPS growth YoY
  min_revenue_growth: 0.25          # 25% minimum revenue growth YoY
  min_operating_margin: 0.15        # 15% minimum operating margin (optional)
  require_qoq_acceleration: true    # Require QoQ growth acceleration

# Backtest settings
backtest:
  start_date: "2020-01-01"
  end_date: "2025-01-27"
  initial_capital: 10_000            # $10,000 (approx 1M yen)
  max_positions: 5                   # Max 5 concurrent positions
  commission: 0.001                  # 0.1% commission
  slippage: 0.001                    # 0.1% slippage
  enable_detailed_log: true          # Enable detailed trade logging

# Experiment / recorder-like metadata
experiment:
  name: "minervini-stage2-baseline"
  strategy_name: "rule-based-stage2"
  rule_profile: "strict-auto-fallback"
  tags:
    - "baseline"
    - "cpu-friendly"
    - "qlib-inspired"

strategy:
  name: "rule-based-stage2"
  entry_profile: "baseline"
  exit_profile: "baseline"
  stop_loss_pct: 0.03
  target_pct: 0.25
  max_hold_days: 90

strategy_profiles:
  rule-based-stage2:
    display_name: "Baseline Stage2"
    short_name: "Baseline"
    title: "Stage2 trend baseline"
    description: "Reference rule-based Stage2 profile with strict auto-fallback entry and exit rules."
    icon_key: "layers"
    result_strategy_name: "rule-based-stage2"
    sort_order: 0
    experiment_name: "minervini-stage2-baseline"
    rule_profile: "strict-auto-fallback"
    entry_profile: "baseline"
    exit_profile: "baseline"
    stop_loss_pct: 0.03
    target_pct: 0.25
    max_hold_days: 90
    tags: ["baseline", "rule-based", "stage2"]
  buffett-quality:
    display_name: "Warren Buffett"
    short_name: "Buffett"
    title: "Quality compounders"
    description: "Looks for durable businesses with stable volatility, rising long-term trends, and patient holding periods."
    icon_key: "brain"
    result_strategy_name: "buffett-quality"
    portrait_asset_key: "buffett"
    sort_order: 10
    experiment_name: "buffett-quality-inspired"
    rule_profile: "quality-compounder"
    entry_profile: "quality"
    exit_profile: "quality"
    min_volume: 300000
    volatility_max: 0.03
    stop_loss_pct: 0.08
    target_pct: 0.3
    max_hold_days: 180
    tags: ["trader-inspired", "quality", "buffett"]
  soros-breakout:
    display_name: "George Soros"
    short_name: "Soros"
    title: "Reflexive breakout momentum"
    description: "Emphasizes decisive breakouts, fast confirmation, and quicker exits when the breakout thesis loses momentum."
    icon_key: "bolt"
    result_strategy_name: "soros-breakout"
    portrait_asset_key: "soros"
    sort_order: 20
    experiment_name: "soros-breakout-inspired"
    rule_profile: "macro-breakout"
    entry_profile: "breakout"
    exit_profile: "breakout"
    min_volume: 300000
    breakout_lookback: 20
    breakout_buffer: 0.01
    volume_spike_ratio: 1.1
    atr_stop_multiplier: 2.0
    stop_loss_pct: 0.06
    target_pct: 0.2
    max_hold_days: 45
    tags: ["trader-inspired", "breakout", "soros"]
  lynch-growth:
    display_name: "Peter Lynch"
    short_name: "Lynch"
    title: "Everyday growth leaders"
    description: "Favors liquid growth names trending near highs while still maintaining a broad, understandable trend structure."
    icon_key: "chart"
    result_strategy_name: "lynch-growth"
    portrait_asset_key: "lynch"
    sort_order: 30
    experiment_name: "lynch-growth-inspired"
    rule_profile: "growth-at-reasonable-trend"
    entry_profile: "growth"
    exit_profile: "growth"
    min_volume: 300000
    near_52w_high_ratio: 0.82
    stop_loss_pct: 0.07
    target_pct: 0.22
    max_hold_days: 120
    tags: ["trader-inspired", "growth", "lynch"]
  minervini-trend:
    display_name: "Mark Minervini"
    short_name: "Minervini"
    title: "Trend template leadership"
    description: "Tracks leadership names near 52-week highs with relative-strength confirmation and tighter breakout risk control."
    icon_key: "target"
    result_strategy_name: "rule-based-stage2"
    portrait_asset_key: "minervini"
    is_current_baseline: true
    sort_order: 40
    experiment_name: "minervini-trend-inspired"
    rule_profile: "volatility-contraction-trend"
    entry_profile: "trend"
    exit_profile: "trend"
    min_volume: 400000
    near_52w_high_ratio: 0.88
    rs_new_high_window: 30
    rs_new_high_threshold: 0.93
    volume_spike_ratio: 1.15
    atr_stop_multiplier: 2.0
    stop_loss_pct: 0.05
    target_pct: 0.25
    max_hold_days: 90
    tags: ["trader-inspired", "trend", "minervini"]
  dalio-balance:
    display_name: "Ray Dalio"
    short_name: "Dalio"
    title: "Balanced trend participation"
    description: "Targets steadier trend participation with volatility control, tighter holding windows, and mean-reversion aware exits."
    icon_key: "balance"
    result_strategy_name: "dalio-balance"
    portrait_asset_key: "dalio"
    sort_order: 50
    experiment_name: "dalio-balance-inspired"
    rule_profile: "balanced-trend"
    entry_profile: "balanced"
    exit_profile: "balanced"
    min_volume: 300000
    volatility_max: 0.02
    ema_tolerance: 0.03
    stop_loss_pct: 0.05
    target_pct: 0.14
    max_hold_days: 30
    tags: ["trader-inspired", "balanced", "dalio"]

# Output settings
output:
  csv_path: "output/screening_results.csv"
  log_path: "output/screening.log"
  log_level: "INFO"

# Performance settings
performance:
  max_workers: 4          # Parallel processing workers
  request_delay: 0.5      # Delay between yfinance requests (seconds)
  retry_attempts: 3       # Number of retries
  timeout: 30             # Request timeout (seconds)
